import { shouldServe, BuildOptions, PrepareCacheOptions } from '@vercel/build-utils';
export declare const version = 3;
export declare function build({ files, entrypoint, workPath, config, meta, }: BuildOptions): Promise<{
    output: import("@vercel/build-utils/dist").Lambda;
}>;
export declare function prepareCache({ workPath }: PrepareCacheOptions): Promise<RuntimeFiles>;
export { shouldServe };
